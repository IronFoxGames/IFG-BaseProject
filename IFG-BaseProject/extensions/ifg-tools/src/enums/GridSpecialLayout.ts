export enum GridSpecialLayout {
    Default = 'Default',                    // Default grid layout
    DefaultShuffled = 'DefaultShuffled',    // Default layout, grid specials shuffled
    Custom = 'Custom'                       // Custom layout of positions and available grid specials
}